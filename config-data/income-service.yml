server:
  port: 9003
spring:
  application:
    name: income-service
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/Income
    username: root
    password: ""  # Keep the password empty for security reasons
